<Window x:Class="Revit.Import.GridImportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Grids from JSON" Height="300" Width="450"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- File selection -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="File: " FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding FileName}" VerticalAlignment="Center" Margin="5,0,0,0"/>
            <Button Content="Browse..." Command="{Binding BrowseCommand}" Margin="10,0,0,0" Padding="8,2"/>
        </StackPanel>

        <!-- Import options -->
        <CheckBox Grid.Row="1" Content="Import Grids" IsChecked="{Binding ImportGrids}" Margin="0,5,0,10"/>

        <!-- Preview section -->
        <TextBlock Grid.Row="2" Text="Grid Preview:" FontWeight="Bold" Margin="0,10,0,5"/>

        <Border Grid.Row="3" 
                Background="LightGray" 
                BorderBrush="Gray" 
                BorderThickness="1">
            <TextBlock Text="Grid preview would appear here" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"/>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,10,0,0">
            <Button Content="Cancel" 
                    Width="80" 
                    Height="25" 
                    Margin="0,0,10,0"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
            <Button Content="Import" 
                    Width="80" 
                    Height="25"
                    IsDefault="True"
                    Command="{Binding ImportCommand}"
                    Click="ImportButton_Click"/>
        </StackPanel>
    </Grid>
</Window>